%ul.secondary-navigation
  %li= link_to("New Creditor", new_creditor_path)
= render :partial => 'index', :locals => { :creditors => @creditors, :pass_through => @pass_through }, :layout => 'shared/block_content'
