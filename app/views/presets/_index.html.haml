- title "Listing Presets"
%table.table
  %tr
    %th Keyphrase
    %th Creditor/Category
    %th
  = render presets, :pass_through => pass_through
.action-bars
  -# A button will be placed in the near future?
  &nbsp;
  = will_paginate presets