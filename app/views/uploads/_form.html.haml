= semantic_form_for @upload, :html => {:multipart => true} do |f|
  = render "shared/error_messages", :target => @upload

  = f.inputs do
    = f.input :xls

  = f.buttons do
    = f.commit_button
    %li= link_to t('formtastic.actions.cancel'), uploads_path
