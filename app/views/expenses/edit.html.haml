%ul.secondary-navigation
  %li= link_to("« Back", bank_account_expenses_path(@bank_account, @pass_through))
= render :partial => 'expenses/edit', :locals => { :expense => @expense, :bank_account => @bank_account, :pass_through => @pass_through }, :layout => 'shared/block_content'

- content_for :sidebar do
  .block
    %h3 Category
    .content
      = render :partial => 'categories/form', :locals => { :category => @category, :pass_through => @pass_through }

  .block
    %h3 Preset
    .content
      = render :partial => 'presets/form', :locals => { :preset => @preset, :pass_through => @pass_through }
