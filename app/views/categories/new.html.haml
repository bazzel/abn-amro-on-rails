%ul.secondary-navigation
  %li= link_to("« Back", categories_path(@pass_through))
  %li= link_to("New Category", new_category_path, :class => 'current')
= render :partial => 'new', :locals => { :category => @category, :pass_through => @pass_through }, :layout => 'shared/block_content'
