= semantic_form_for(category) do |f|
  = render "shared/error_messages", :target => category

  = postback_url(:controller => 'categories')
  = hash_as_hidden_fields(pass_through)

  = f.inputs do
    = f.input :name
    = category_parent_input f, category

  = f.buttons do
    = f.commit_button
    - unless must_be_a_sidebar?(:controller => 'categories')
      -# No Cancel link if displayed in the sidebar.
      %li= link_to "Cancel", categories_path(pass_through)
