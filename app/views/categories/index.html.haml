%ul.secondary-navigation
  %li= link_to("New Category", new_category_path(@pass_through))
  %li= link_to("Main Categories", categories_path(:roots => true), :class => params[:roots] && 'current')
  %li= link_to("Subcategories", categories_path, :class => !params[:roots] && 'current')
= render :partial => 'index', :locals => { :categories => @categories, :pass_through => @pass_through }, :layout => 'shared/block_content'
